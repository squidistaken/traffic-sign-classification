[project]
name = "traffic-sign-classification"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "requests",
    "numpy",
    "pillow",
    "logging>=0.4.9.6",
    "typing>=3.10.0.0",
]
